{
    "name": "myworkspace",
    "version": "0.1.0",
    "private": true,
    "devDependencies": {
        "@bazel/esbuild": "^3.2.0",
        "@bazel/typescript": "^3.2.0"
    },
    "scripts": {
        "build": "bazel build //...",
        "test": "bazel test //..."
    },
    "dependencies": {
        "typescript": "^4.1.5"
    }
}
